// API constants
import 'dart:ui';

const String API_BASE_URL = 'https:/music.ghurifiri.world/api';

// UI constants
const double DEFAULT_PADDING = 16.0;
const double BORDER_RADIUS = 16.0;

// Animation durations
const Duration ANIMATION_DURATION_SHORT = Duration(milliseconds: 200);
const Duration ANIMATION_DURATION_MEDIUM = Duration(milliseconds: 500);
const Duration ANIMATION_DURATION_LONG = Duration(milliseconds: 800);

// Font sizes
const double FONT_SIZE_SMALL = 12.0;
const double FONT_SIZE_MEDIUM = 14.0;
const double FONT_SIZE_LARGE = 16.0;
const double FONT_SIZE_XLARGE = 20.0;
const double FONT_SIZE_XXLARGE = 24.0;

// Storage keys
const String STORAGE_RECENT_SONGS = 'recent_songs';
const String STORAGE_FAVORITE_SONGS = 'favorite_songs';
const String STORAGE_SETTINGS = 'user_settings';

// Default settings
const bool DEFAULT_SHOW_LYRICS = true;
const bool DEFAULT_DOWNLOAD_ON_WIFI_ONLY = true;
const int DEFAULT_AUDIO_QUALITY = 2; // 0: Low, 1: Medium, 2: High

// Page sizes for pagination
const int PAGE_SIZE = 20;

const Color primaryColor = Color(0xFF6C63FF);
const Color primaryLightColor = Color(0xFF9D97FF);
const Color primaryDarkColor = Color(0xFF4A3FCB);
const Color secondaryColor = Color(0xFFFF6584);
const Color secondaryLightColor = Color(0xFFFF9BB3);
const Color secondaryDarkColor = Color(0xFFD8325F);
const Color backgroundColor = Color(0xFF121212);
const Color surfaceColor = Color(0xFF1E1E1E);
const Color cardColor = Color(0xFF252525);
const Color primaryTextColor = Color(0xFFFFFFFF);
const Color secondaryTextColor = Color(0xFFB3B3B3);
